# 玄剑公会 QQ 群机器人 —— 项目规划书（Plan）

> 项目路径：`xuanjian-group-bot/`（独立子项目，已加入主仓库 `.gitignore`，不与官网主仓库混管）
> 文档版本：v1.0　|　状态：规划阶段（尚未分配 QQ 号 / 尚未部署）

---

## 1. 项目背景

玄剑公会有自己的官方网站（xuanjian.top）与 Minecraft 游戏模组联动体系，但公会成员的日常交流集中在 QQ 群。目前官网的动态、贡献点经济、处分/档案等信息与 QQ 群是割裂的。

本项目的目标是：**搭建一个能接入玄剑公会 QQ 群的群机器人**，把官网能力与 QQ 群打通，并承担群内日常管理、查询、娱乐等功能。

关键约束与前提（按用户指示）：
- 后续会**分配一个专属 QQ 号**给机器人（用于 NapCatQQ 协议端登录）。
- 机器人将部署在**一台新的云服务器**上，与现官网服务器（115.190.153.44）相互独立。
- 基于 **NapCatQQ** 框架搭建，本项目只负责业务逻辑与运维，不重新造协议层。

---

## 2. 技术选型

| 层 | 选型 | 说明 |
|---|---|---|
| 协议端 | **NapCatQQ**（NapNeko/NapCatQQ） | 基于 NTQQ 的现代化 Bot 协议端，实现 OneBot 协议；支持 Linux，内存轻量（50~100MB），Core/Adapter 架构 |
| 对接 SDK | **node-napcat-ts**（HkTeamX/node-napcat-ts） | NapCat 官方推荐的 TypeScript SDK，`NCWebsocket` 正向连接 NapCat，所有接口/返回类型完整，MIT 许可 |
| 业务语言 | **TypeScript + Node.js** | 与官网后端（Node.js）同技术栈，便于复用/移植工具函数 |
| 运行环境 | **新云服务器**（Linux x64） | NapCat + 本项目 Node 服务 + PM2 守护 |
| 配置/持久化 | `.env` + SQLite 或 JSON | 机器人自身配置与轻量状态存储；官网数据走 HTTP 拉取，不直接连官网数据库 |
| 进程管理 | PM2 | 与官网一致 |

> 参考：
> - NapCatQQ 文档：https://napneko.github.io/
> - NapCatQQ 仓库：https://github.com/NapNeko/NapCatQQ
> - node-napcat-ts 文档：https://node-napcat-ts.huankong.top
> - node-napcat-ts 仓库：https://github.com/HkTeamX/node-napcat-ts

---

## 3. 总体架构

```
             QQ 群
              │
              ▼
   ┌───────────────────┐   OneBot 协议(正向 WS)   ┌──────────────────────────┐
   │   NapCatQQ 协议端  │ ◄──────────────────────► │  群机器人服务 (本项目)      │
   │  （用分配的QQ号登录）│                           │  node-napcat-ts + TS/Node  │
   └───────────────────┘                           │  ────────────────────────  │
              ▲                                    │  · 指令/事件处理器           │
              │                                    │  · 群管理/审核              │
              │                                    │  · 官网对接模块              │
              │                                    │  · 娱乐/签到/查询            │
              │                                    └──────────┬───────────────┘
              │                                               │ HTTP
              │                                               ▼
              │                                    ┌──────────────────────────┐
              │                                    │  官网 xuanjian.top (独立)  │
              │                                    │  /api/*（贡献点/档案/处分…） │
              │                                    └──────────────────────────┘
              └─────────────── 同一台新云服务器，双进程部署
```

- **NapCatQQ** 负责与 QQ 服务器的通信，暴露 OneBot 接口。
- **群机器人服务** 通过 `node-napcat-ts` 的 `NCWebsocket` 正向连接 NapCat，接收群消息/事件，执行业务逻辑，调用官网 API 或本地存储。
- 官网数据通过**官网已开放/新增的 HTTP 接口**读取，**不直连官网数据库**（解耦，避免影响官网）。

---

## 4. 功能规划

### 4.1 一期（MVP · 群管理 + 基础查询）
- **入群欢迎 / 退群告别**：新成员进群发欢迎并引导查看群规、官网；退群记录。
- **群规与帮助**：`#帮助` 输出指令清单。
- **官网联动查询**（经官网 API）：
  - `#档案 <ID/昵称>` → 拉取 GMIRS 档案摘要（贡献点、代系、处分状态）。
  - `#处分 <ID>` → 查询处分记录。
  - `#签到排行` / `#贡献点排行` → 官网排行榜 Top N。
  - `#在线` → 官网模组在线玩家（若接口开放）。
- **关键词/定时播报**：官网日报、决策更新同步到群（由官网推送或机器人轮询）。

### 4.2 二期（群运营增强）
- **群成员管理**：管理员指令（禁言/踢出/改名片）的权限封装。
- **群活跃统计**：每日发言排行，与官网贡献点联动（可选）。
- **人工客服转接 / @机器人 问答**：基于关键词的 FAQ。
- **抽奖/签到**：群内每日签到，与官网签到打通（需官网开放写接口 + 安全鉴权）。

### 4.3 三期（深度联动，可选）
- 官网用户与 QQ 绑定，群内可直接操作官网（转账申请、任务领取提醒等）。
- 与模组联动：群内播报游戏内事件。

> 一、二、三期仅作规划，实际按需裁剪。MVP 以 **4.1** 为主。

---

## 5. 目录结构（规划）

```
xuanjian-group-bot/
├── Plan.md                  # 本规划
├── README.md                # 部署/使用说明
├── .gitignore
├── .env.example             # 配置模板（QQ 号、NapCat WS 地址、官网 API 等）
├── package.json
├── tsconfig.json
├── src/
│   ├── index.ts             # 入口：连接 NapCat、注册插件
│   ├── config.ts            # 配置读取
│   ├── core/
│   │   ├── bot.ts           # NCWebsocket 封装与事件分发
│   │   ├── command.ts       # 指令注册/解析
│   │   └── permission.ts    # 权限/管理员判定
│   ├── handlers/
│   │   ├── group.ts         # 入群/退群/消息处理
│   │   ├── member.ts        # 群成员管理指令
│   │   └── faq.ts           # 关键词/FAQ
│   ├── services/
│   │   ├── officialApi.ts   # 官网 HTTP 客户端（贡献点/档案/处分/排行）
│   │   └── notify.ts        # 日报/决策播报
│   └── utils/
├── scripts/
│   └── deploy.sh            # 服务器部署脚本
└── napcat/                  # NapCatQQ 协议端（部署时下载，不入库）
    └── (NapCat 二进制/配置)
```

---

## 6. 依赖与外部接口

### 6.1 运行时依赖
- Node.js ≥ 18（推荐 20/22）
- PM2
- `node-napcat-ts`（npm 包）
- 可选：`node-cron`（定时播报）、`better-sqlite3`（本地状态）

### 6.2 官网依赖接口（需确认/补充）
本项目要读取官网数据，需官网提供（或已提供）**公开/带鉴权的只读接口**：
- GMIRS 档案：`GET /api/gmirs/query`、`GET /api/gmirs/user/:id`（已存在）
- 处分：`GET /api/discipline/query`（已存在）
- 排行榜：`GET /api/rankings/contribution`、`/api/rankings/checkin`（已存在）
- 在线玩家：`GET /api/mod/online`（已存在）
- 日报/决策：`GET /api/posts?type=daily/decision`（已存在）

> **待官网补充**：为机器人提供独立的**只读 API Token** 或专用接口，避免机器人滥用公开接口/拉全量数据。

---

## 7. 部署方案（新云服务器）

1. **开通新云服务器**（Linux x64，建议 ≥1C2G，腾讯/阿里均可）。
2. **安装 Node.js + PM2 + Git**。
3. **部署 NapCatQQ 协议端**：
   - 下载最新 Release，解压到 `napcat/`。
   - 用分配的 QQ 号扫码/密码登录，配置正向 WebSocket（监听端口，如 3001）。
4. **部署本项目**：
   - `git clone`（或 scp）本项目到服务器。
   - `npm install`，配置 `.env`（QQ 号、NapCat WS 地址、官网 API 地址与 Token）。
   - `pm2 start` 启动机器人服务。
5. **安全**：
   - 官网接口只读 Token 使用环境变量，不入库。
   - 机器人所在服务器仅开放必要端口（NapCat WS、SSH），官网服务器不开放给公网机器人直连 DB。

---

## 8. 里程碑

| 阶段 | 内容 | 依赖 |
|---|---|---|
| M0 | 完成 Plan、建立项目骨架、跑通 NapCat+node-napcat-ts 最小连接 | 新服务器 + 分配的 QQ 号 |
| M1 | MVP：入群欢迎、帮助、官网档案/处分/排行查询 | 官网只读接口可用 |
| M2 | 群管理指令、日报/决策播报 | — |
| M3 | 深度联动（绑定、群签到等） | 官网开放安全写接口 |

---

## 9. 风险与待确认事项

1. **QQ 号稳定性**：QQ 机器人账号有被风控/封禁风险，需专用小号，登录后保持在线。
2. **NapCatQQ 兼容性**：NapCat 依赖 NTQQ 版本，新 QQ 号需测试登录稳定性。
3. **官网接口鉴权**：需官网新增只读 Token 机制，避免机器人调用影响官网性能/安全。
4. **官网与机器人解耦**：机器人不直连官网数据库，统一走 HTTP。
5. **合规**：机器人功能需符合 QQ 平台与 NapCat 使用规范。

---

## 10. 下一步

1. 确认**新云服务器**信息（IP/系统/配置），开通后做 M0。
2. 确认**分配的机器人 QQ 号**。
3. 官网侧准备**机器人只读 API Token**。
4. 搭建项目骨架并跑通 NapCat + node-napcat-ts 最小链路。
